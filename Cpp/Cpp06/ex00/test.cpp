#include <iostream>
#include <string>
#include <sstream>

int main()
{
	//double a = strtod("nan", NULL);
	//double b = strtod("inf", NULL);
	//double c = strtod("+inf", NULL);
	//double d = strtod("-inf", NULL);

	//std::cout << a << std::endl;
	//int e = static_cast<int>(a);
	//std::cout << "int : " << e << std::endl;
	//char f = static_cast<char>(a);
	//std::cout << "char : " << f << std::endl;
	//float g = static_cast<float>(a);
	//std::cout << "float : " << g << std::endl;
	char a = -1;

	std::cout << a << std::endl;
}